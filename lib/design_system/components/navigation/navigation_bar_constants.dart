import 'package:flutter/widgets.dart';

class NavigationBarConstants {
  static const double containerHeight = 50;
  static final BorderRadius containerRadius =
      BorderRadius.circular(containerHeight / 2);
}
